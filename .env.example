# PPI Wallet Admin — copy to .env to override defaults
# NEVER commit .env

# Backend API URL. Leave blank to use mock data.
# Production: https://ppi-wallet-api.onrender.com
VITE_API_URL=

# Base path for the deployed site. Default '/' for local; '/ppi-wallet-admin/' for GitHub Pages.
VITE_BASE_PATH=/
